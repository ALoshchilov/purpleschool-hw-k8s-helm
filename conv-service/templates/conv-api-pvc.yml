apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: "{{ .Values.common.name }}-{{ .Values.api.name }}-pvc"
spec:
  resources: {{ .Values.api.pvc | toYaml | nindent 4 }}
  volumeMode: Filesystem
  accessModes:
    - ReadWriteOnce
